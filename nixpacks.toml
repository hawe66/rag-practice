[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = [
  "poetry config virtualenvs.in-project true",
  "poetry install --no-dev --no-interaction --no-ansi"
]

[phases.build]
cmds = []

[start]
cmd = "cd week9-api-deployment && poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"